import React from "react";
import ReactDOM from "react-dom";

const main = React.createElement("h1",{},"Hello this is world");
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(main);